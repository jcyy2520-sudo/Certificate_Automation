/*
 * The interface is server-rendered Blade. The only client-side behaviour lives
 * inline on the pages that need it (the survey stepper and the sidebar toggle),
 * so this bundle stays deliberately empty rather than shipping a library the
 * application never calls.
 */
